<div id="sign_in_form">
<h1>Welcome Back</h1>

<%= form_for :dog_session, { url: "/sessions", method: "post" } do |f| %>

<% if @dog_session.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dog_session.errors.count, "error") %> prohibited this dog from being signed in:</h2>

      <ul>
      <% @dog_session.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<%= f.label "Email or Username" %><br/>
<%= f.text_field :username, placeholder: "Username" %>
<br>
<%= f.label "Password" %><br/>
<%= f.password_field (:password), placeholder: "Password" %>
<br>
<br>
<%= f.submit "Log In", class: "call_to_action" %>

<% end %>
</div>
