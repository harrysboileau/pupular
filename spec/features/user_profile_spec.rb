require 'spec_helper'

feature 'UserProfile' do
  scenario "Dog can fill out profile after signing up"
  scenario "Dog can skip profile after signing up"
  scenario "If dog has not filled out profile they will be prompted when they press the profile button"
  scenario "Dog can see their own profile when they click the profile button"
  scenario "Dog can edit their own profile"
end
